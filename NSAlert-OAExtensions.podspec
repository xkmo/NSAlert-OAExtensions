Pod::Spec.new do |s|
        s.name                  = 'NSAlert-OAExtensions'
        s.version               = '1.0'
        s.summary               = 'Modification of Omni Groups NSAlert-OAExtensinos that can live standa lone.'
        s.homepage              = 'https://github.com/loghound/Simple-NSAlert-with-Blocks'
        s.author                = 'loghound'
        s.source_files          = '*.{h,m}'
        s.source                = { :git => 'https://github.com/xkmo/NSAlert-OAExtensions' }
end
